'use strict';

module.exports = function(Repartitionaff) {
//var connector = Repartitionaff.getDataSource().connector;
	// Repartitionaff.observe('loaded', function logQuery(ctx, next) {

    //         try {
    //         var fTotal = ctx.instance.__data.reduce(function(first,second){
    //         return first + second; }, 0);
    //         ctx.instance.__data.forEach(function(item){
    //             item.pourcentage = 100 * item.Debit / fTotal;
    //         } );    
    //         } catch (error) {
                
    //         }
            
		
	// 	//console.log( JSON.stringify(ctx.instance.__data));
	// 	next();
	// });

};
